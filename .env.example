NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=

# Server-only. NEVER prefix with NEXT_PUBLIC_, never import in a Client
# Component. Bypasses RLS — only used server-side to create/reset student
# accounts (see src/lib/supabase/admin.ts). Get it from Supabase Dashboard
# > Project Settings > API Keys > "secret" key.
SUPABASE_SERVICE_ROLE_KEY=
